#include "task2.h"
#include <iostream>
#include <cstdlib>
#include <ctime>

/*void input_check(std::string &input_value, int &value) {
    for (auto i: input_value)
        if (i < '1' || i > '9') throw std::invalid_argument("value is not from 1 to 9!");
    value = std::stoi(input_value);
}

void new_game_field(int* &field)
{
    int i =std::rand() % 9;
    //field[i].fish = new Fish();
}
*/
void task2()
{
    //int field[9];
    //std::srand(std::time(nullptr));
};